import React from "react";

const Home = props => (
  <div>
    <h1 className="cover-heading">Your Secrets, Your Profits</h1>
    <p className="lead">
      Whisper allows users to post and sell secrets. Secrets are what makes the
      world go round. Profits are what makes you go around.
    </p>
  </div>
);

export default Home;
